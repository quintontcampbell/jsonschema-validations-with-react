{
  "type": "module",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "build:client": "yarn workspace jsonSchema-validations-with-react-client build",
    "dev": "yarn workspace jsonSchema-validations-with-react-server dev",
    "dev:debug": "yarn workspace jsonSchema-validations-with-react-server dev:debug",
    "heroku-postbuild": "yarn workspace jsonSchema-validations-with-react-client build",
    "start": "yarn workspace jsonSchema-validations-with-react-server start"
  },
  "engines": {
    "node": "^14.15"
  }
}
